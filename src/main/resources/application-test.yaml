spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb  # 테스트용 MySQL 데이터베이스 URL
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect